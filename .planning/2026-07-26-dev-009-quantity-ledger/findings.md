# DEV-009 发现

## 前置门禁

- 当前基线为 `main@c5af084`，DEV-008（PR #8，`33fc357`）已合并。
- `validate` 通过：93 个规格版本、389 个 PRD 来源条目。
- `source-status` 为 SOURCE CURRENT；`impact` 无影响项。
- `ATC-QTY-001@1.0.0` 尚不存在，不能直接进入实现。
- 用户于 2026-07-26 选择 DEV-009 方向为 `ATC-QTY-001 不可变数量流水与并发预留`。

## 来源语义（PRD）

- OPS-QTY-001：必须使用不可变数量流水记录收货、产出、预留、分配、消耗、归还、损耗和处置。
- OPS-QTY-002：必须按对象配置计量维度、单位、测量方法、精度、允许损耗和守恒公差。
- OPS-QTY-003：必须阻止负余额、超分配和无依据跨物理量换算。
- OPS-QTY-004：必须通过冲销和重记更正已过账数量流水（不可原地修改）。
- OPS-LIN-001：数量流水与产品语义关系、物理来源关系、任务使用关系、保管责任链分别管理。
- BP-018：追踪粒度、计量维度和守恒精度按方法风险配置；禁止对非破坏性或不可合理计量场景制造伪精确数量。
- 派生样品/试样状态机：`已计划 → 已创建 → 可用 → 已预留 → 部分使用 → 已耗尽`（10.7/11.5 节）。
- 10.7 节：数量流水记录投入、产出、损耗、预留、分配、消耗和结余；超分配、负余额、不兼容单位被阻止。

## 阻断决定

- **OD-010（各样品类型的计量维度、精度、损耗和容差）在 `spec/decisions/` 中完全不存在**，是 OPS-QTY-002 的直接前置；等同于 DEV-008 的 OD-027 缺口，必须由用户批准最小口径后追加 `OD-010@1.0.0`。

## 可依赖的既有批准决定

- `OD-005@1.0.0`（approved/decided）：DEV-006 异常决定边界。
- `OD-009@1.0.0`（approved/decided）：收到实物唯一识别粒度——数量流水的对象锚点。
- `OD-031@1.0.0`、`OD-035@1.0.0`：approved/decided。
- 平台依赖沿用 DEV-008 同款：`ATC-PLT-003@1.0.0`、`ED-001@2.0.0`、`SEC-AUTH-001@1.0.0`、`SEC-AUD-001@2.0.0`、`NFR-ARCH-001@2.0.0`、`OD-002@1.0.0`（所有 Story 必须直接固定的单集团边界）。

## 现状核对

- `src/`、`contracts/`、`apps/` 无任何 QuantityLedger/Reservation 实现（仅二进制误命中）。
- `spec/` 无 QTY 相关规格；OPS-QTY 来源条目已在已确认 PRD 来源基线内。
- 就绪报告中所有 READY 任务卡均已交付；DEV-009 需全新垂直切片（复用 Scope 模块建立的组合协议、独立 schema、平台事务/审计/Outbox 端口范式）。
