# DEV-011 纺织样品需求和互斥裁样门禁

## 目标

在不推进发布、Seal 或部署的前提下，从 PRD 收敛 `ATC-TEX-001` 的最小业务切片；只有任务卡 READY 后才实现纺织样品需求的版本固定计算输入/结果、样品充足性校验与互斥破坏关系的失败关闭门禁。

## 阶段

1. [completed] 完成规格、来源、影响、纺织语义、行业包激活模式和实现边界核对。
2. [completed] 按全流程授权收敛最小业务基线（纺织降级为契约切片，用户明确选择）（遇未决 OD 需实质决策时停下询问用户）。
3. [completed] 创建批准的后继规格与任务卡，生成派生物并使精确版本任务卡返回 READY。
4. [completed] 在 allowed_paths 内实现领域逻辑、公共契约、持久化/API、审计和门禁端口。
5. [completed] 补齐正向、反向、边界、权限、并发、恢复、审计和回归测试。
6. [completed] 执行完整门禁，二次生成 written=0；已按授权自动提交、PR #11、CI 全绿并 squash 合并为 `main@19ab75b`。

## 约束

- 本任务不修改 Release baseline，不创建 Seal、tag、GitHub Release 或部署。
- PRD 只读；`generated/spec/` 只允许由生成器写入。
- 未知适用性、规则、版本或权限默认阻断。
- BP-019：行业包不得另建平行状态机或绕过平台关口。
- BP-018：不得制造伪精确数量。
- 用户已于 2026-07-26 给出全流程授权（按 backlog 顺序、基线自行收敛、CI 全绿后自动合并；未决 OD 需实质决策时停下）。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| `ATC-TEX-001@1.0.0` 不存在，ready 返回错误 | 1 | 作为预期缺口记录；先依据现有来源起草任务卡。 |
