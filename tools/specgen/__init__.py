"""OpenLIMS specification compiler.

The package intentionally uses only the Python standard library so the
requirements governance checks can run before an application stack exists.
"""

__version__ = "0.1.0"
